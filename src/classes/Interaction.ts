export default class Interaction {
    description: string;
    event: string;
    constructor(description: string, event: string) {
        this.description = description;
        this.event = event;
    }
}