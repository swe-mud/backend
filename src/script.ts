
import NonPlayerCharacter from "./classes/NonPlayerCharacter";
import Player from "./classes/Player"
import Room from "./classes/Scene";

let player = new Player(a,b,c,d,e);
let professor1 = new NonPlayerCharacter(a,b,c,d,e,f);
let room1 = new Room(a,b);
let room2 = new Room(a,b);
playerInput= ;

function(): void {
    if (player.position == room1) {
        room1.description;
        room1.actions
        if (action == actions[0]) {
            actions[0].consequence;
        }
        else if (action == actions[1]) {
            actions[1].consequence;
        }
    }



}